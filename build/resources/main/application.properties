spring.application.name=Hospital_Management

# Server Port
server.port=803

# ============================================================
# Database Configuration - MySQL Setup
# ============================================================
spring.datasource.url=jdbc:mysql://localhost:3306/hospitaldb?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=Shiva@543

# ============================================================
# Alternative: H2 In-Memory Database (Uncomment to use H2)
# ============================================================
# spring.datasource.url=jdbc:h2:mem:hospitaldb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=

# H2 Console Settings
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA / Hibernate Properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

# Thymeleaf Cache (disable during dev)
spring.thymeleaf.cache=false

# Mail Sender Configuration (default parameters with fallback handling in EmailService)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=gillshubhaman46@gmail.com
spring.mail.password=uejxlrdpcobdnely
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

