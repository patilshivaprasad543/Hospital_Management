# Disabled. This project uses Gradle. The old Maven publish job requested
# packages:write and could trigger GitHub permission prompts.
name: Maven Package (disabled)

on:
  workflow_dispatch:

jobs:
  skipped:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - run: echo "Maven publish is disabled. This app uses Gradle."
